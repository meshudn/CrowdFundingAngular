import { MakeUserImagePipe } from './make-user-image.pipe';

describe('MakeUserImagePipe', () => {
  it('create an instance', () => {
    const pipe = new MakeUserImagePipe();
    expect(pipe).toBeTruthy();
  });
});
